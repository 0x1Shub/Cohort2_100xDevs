// Convert an array of numbers into a new array with every element Multiplied by 2

const numbers = [1, 2, 3, 4, 5];

const doubleArray = numbers.map((numbers) => numbers * 2);
console.log(doubleArray);
